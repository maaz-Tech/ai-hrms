# Runtime deps. Kept slim so the app fits Vercel's serverless size limit.
# Heavy/optional libs (sentence-transformers, torch, qdrant-client) are NOT
# included — the vector store falls back to a hashing embedder, and Gemini
# powers the AI features. Install them locally only if you want model-based
# embeddings: pip install sentence-transformers qdrant-client
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
python-dotenv==1.0.1
email-validator==2.2.0
faker==33.1.0
pypdf==5.1.0
reportlab==4.2.5
google-genai==1.9.0
psycopg[binary]==3.2.3
httpx==0.28.1
